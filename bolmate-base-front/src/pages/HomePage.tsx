const HomePage = () => (
  <section className="panel">
    <h2>Welcome</h2>
    <p>
      This boilerplate pairs a Flask API with a React frontend. Use the navigation to explore the sample users
      workflow and verify connectivity via the healthcheck endpoint.
    </p>
  </section>
)

export default HomePage
